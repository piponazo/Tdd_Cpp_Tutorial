include_directories(${PROJECT_BINARY_DIR})    # To find the export file generated by generate_export_header

add_executable(unitTests
  MainUnitTests.cpp
)

target_link_libraries(unitTests PRIVATE GTest::GTest)

add_test(NAME unitTests COMMAND $<TARGET_FILE:unitTests>)
